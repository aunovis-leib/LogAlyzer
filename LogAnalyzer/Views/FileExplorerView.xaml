<UserControl x:Class="LogAnalyzer.Views.FileExplorerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:LogAnalyzer.Views"
             xmlns:vm="clr-namespace:LogAnalyzer.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="200">
    <Border Background="#F8F8F8" BorderBrush="#CCC" BorderThickness="0,0,1,0">
        <DockPanel>
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Top" Margin="2">
                <Button Content="↑" Command="{Binding GoUpCommand}" Width="24" Height="24" Margin="0,0,4,0" />
                <TextBlock Text="{Binding CurrentPath}" VerticalAlignment="Center" FontSize="10" Foreground="#888" TextTrimming="CharacterEllipsis" Width="Auto" ToolTip="{Binding CurrentPath}"/>
            </StackPanel>
            <ListView x:Name="ExplorerListView"
                      ItemsSource="{Binding Items}"
                      SelectedItem="{Binding SelectedItem, Mode=TwoWay}"
                      SelectionMode="Extended"
                      KeyDown="ExplorerListView_KeyDown"
                      MouseDoubleClick="ListBox_MouseDoubleClick">
                <ListView.ContextMenu>
                    <ContextMenu>
                        <MenuItem Header="Im Explorer öffnen" Command="{Binding OpenInExplorerCommand}" CommandParameter="{Binding SelectedItem}">
                            <MenuItem.Icon>
                                <TextBlock Text="📁" />
                            </MenuItem.Icon>
                        </MenuItem>
                        <MenuItem Header="Datei leeren" Command="{Binding ClearFileCommand}" CommandParameter="{Binding SelectedItem}">
                            <MenuItem.Icon>
                                <TextBlock Text="🗑" />
                            </MenuItem.Icon>
                        </MenuItem>
                    </ContextMenu>
                </ListView.ContextMenu>
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsLoaded}" Value="True">
                                <Setter Property="Background" Value="#22007ACC" />
                                <Setter Property="FontWeight" Value="SemiBold" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </ListView.ItemContainerStyle>
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Name" Width="auto" DisplayMemberBinding="{Binding Name}" />
                        <GridViewColumn Header="Geändert" Width="120" DisplayMemberBinding="{Binding LastModifiedDisplay}" />
                        <GridViewColumn Header="Größe" Width="80" DisplayMemberBinding="{Binding SizeDisplay}" />
                    </GridView>
                </ListView.View>
            </ListView>
        </DockPanel>
    </Border>
</UserControl>
