<UserControl x:Class="LogAlyzer.Views.FileExplorerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:LogAlyzer.Views"
             xmlns:vm="clr-namespace:LogAlyzer.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="200">
    <Border Background="#F8F8F8" BorderBrush="#CCC" BorderThickness="0,0,1,0">
        <DockPanel>
            <Grid DockPanel.Dock="Top" Margin="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Button Grid.Column="0" Content="↑" Command="{Binding GoUpCommand}" Width="24" Height="24" Margin="0,0,4,0" />
                <ComboBox Grid.Column="1"
                          ItemsSource="{Binding ExplorerRootFolderHistory}"
                          SelectedItem="{Binding SelectedHistoryPath, Mode=TwoWay}"
                          Text="{Binding SelectedHistoryPath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                          ToolTip="{Binding CurrentPath}"
                          IsEditable="True">
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0"
                                           Text="{Binding}"
                                           VerticalAlignment="Center"
                                           TextTrimming="CharacterEllipsis" />
                                <Button Grid.Column="1"
                                        Content="×"
                                        Width="22"
                                        Height="22"
                                        Padding="0"
                                        Margin="6,0,0,0"
                                        Focusable="False"
                                        Command="{Binding DataContext.RemoveHistoryCommand, RelativeSource={RelativeSource AncestorType={x:Type ComboBox}}}"
                                        CommandParameter="{Binding}" />
                            </Grid>
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>
            </Grid>
            <ListView x:Name="ExplorerListView"
                      ItemsSource="{Binding Items}"
                      SelectedItem="{Binding SelectedItem, Mode=TwoWay}"
                      SelectionMode="Extended"
                      KeyDown="ExplorerListView_KeyDown"
                      MouseDoubleClick="ListBox_MouseDoubleClick">
                <ListView.ContextMenu>
                    <ContextMenu>
                        <MenuItem Header="Im Explorer öffnen" Command="{Binding OpenInExplorerCommand}" CommandParameter="{Binding SelectedItem}">
                            <MenuItem.Icon>
                                <TextBlock Text="📁" />
                            </MenuItem.Icon>
                        </MenuItem>
                        <MenuItem Header="Datei leeren" Command="{Binding ClearFileCommand}" CommandParameter="{Binding SelectedItem}">
                            <MenuItem.Icon>
                                <TextBlock Text="🗑" />
                            </MenuItem.Icon>
                        </MenuItem>
                    </ContextMenu>
                </ListView.ContextMenu>
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsLoaded}" Value="True">
                                <Setter Property="Background" Value="#22007ACC" />
                                <Setter Property="FontWeight" Value="SemiBold" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </ListView.ItemContainerStyle>
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Name" Width="auto" DisplayMemberBinding="{Binding Name}" />
                        <GridViewColumn Header="Geändert" Width="120" DisplayMemberBinding="{Binding LastModifiedDisplay}" />
                        <GridViewColumn Header="Größe" Width="80" DisplayMemberBinding="{Binding SizeDisplay}" />
                    </GridView>
                </ListView.View>
            </ListView>
        </DockPanel>
    </Border>
</UserControl>
